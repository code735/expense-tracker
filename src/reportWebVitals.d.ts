declare function reportWebVitals(): void;

// If reportWebVitals takes any parameters or returns a value, you should specify that in the declaration.
// For example:
// declare function reportWebVitals(arg: SomeType): ReturnType;
